import { Routes } from '@angular/router';
import { RegistrationManagerComponent } from './features/registration/components/registration-manager/registration-manager.component';

export const routes: Routes = [
  {
    path: '',
    component: RegistrationManagerComponent
  }
];
